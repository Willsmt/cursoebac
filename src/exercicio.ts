// Função de multiplicação
function multiplicar(a: number, b: number): number {
  return a * b;
}

// Função de saudação
function saudacao(nome: string): string {
  return "Olá " + nome;
}

console.log(multiplicar(5, 3));
console.log(saudacao("Willians"));
